<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.complitex.flexbuh.service.dictionary.RegionBean">
    <resultMap id="regionResultMap" type="org.complitex.flexbuh.entity.dictionary.Region"
               extends="org.complitex.flexbuh.entity.dictionary.AbstractPeriodDictionary.periodDictionaryResultMap">
        <result property="code" column="code"/>
    </resultMap>

    <insert id="insertRegion" parameterType="org.complitex.flexbuh.entity.dictionary.Region" keyProperty="id" useGeneratedKeys="true">
        insert into `region` (`upload_date`, `begin_date`, `end_date`, `code`, `name_ru`, `name_uk`)
        	values (#{uploadDate}, #{beginDate}, #{endDate}, #{code}, #{nameRu}, #{nameUk})
    </insert>

	<update id="updateRegion" parameterType="org.complitex.flexbuh.entity.dictionary.Region">
        update `region` set `upload_date`=#{uploadDate}, `begin_date`=#{beginDate}, `end_date`=#{endDate}, `code`=#{code}, `name_ru`=#{nameRu}, `name_uk`=#{nameUk}
        where `id` = #{id}
    </update>

	<select id="selectAllRegions" resultMap="regionResultMap">
        select * from `region`
    </select>

    <select id="selectAllRegionsCount" resultType="int">
        select count(*) from `region`
    </select>

	<select id="selectRegionByCode" parameterType="int" resultMap="regionResultMap">
        select * from `region` where `code` = #{code}
    </select>

	<select id="selectRegions" parameterType="org.complitex.flexbuh.entity.AbstractFilter" resultMap="regionResultMap">
        select * from `region` order by `code` limit #{first}, #{count}
    </select>

	<select id="selectRegion" parameterType="long" resultMap="regionResultMap">
        select * from `region` where `id` = #{id}
    </select>
</mapper>