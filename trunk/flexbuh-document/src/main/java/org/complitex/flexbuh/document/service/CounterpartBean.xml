<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.complitex.flexbuh.document.service.CounterpartBean">
    <resultMap id="counterpartResultMap" type="org.complitex.flexbuh.document.entity.Counterpart">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="person_profile_id" property="personProfileId"/>
        <result column="hk" property="hk"/>
        <result column="last_name" property="lastName"/>
        <result column="first_name" property="firstName"/>
        <result column="middle_name" property="middleName"/>
        <result column="hloc" property="hloc"/>
        <result column="htel" property="htel"/>
        <result column="hnspdv" property="hnspdv"/>
    </resultMap>

    <select id="selectCounterpart" parameterType="long" resultMap="counterpartResultMap">
        select * from `counterpart` where `id` = #{id}
    </select>

    <sql id="selectCounterpartsWhere">
        <where>
           <!-- <if test="object.hk != null">c.`hk` like concat('%', #{object.hk}, '%')</if>
            <if test="object.hname != null">
                and concat_ws(' ',c.`last_name`, c.`first_name`, c.`middle_name`) like concat('%', #{object.hname}, '%')
            </if>
            <if test="object.hloc != null">and c.`hloc` like concat('%', #{object.hloc}, '%')</if>
            <if test="object.htel != null">and c.`htel` like concat('%', #{object.htel}, '%')</if>
            <if test="object.hnspdv != null">and c.`hnspdv` like concat('%', #{object.hnspdv}, '%')</if>-->
            and c.`session_id` = #{object.sessionId}
            and c.`person_profile_id` = #{object.personProfileId}
        </where>
    </sql>

    <select id="selectCounterparts" parameterType="org.complitex.flexbuh.common.entity.FilterWrapper"
            resultMap="counterpartResultMap">
        select c.*, concat_ws(' ', c.`last_name`, c.`first_name`, c.`middle_name`) as `hname` from `counterpart` c
            <include refid="selectCounterpartsWhere"/>
            order by ${sortProperty}
            <choose><when test="ascending">asc</when><otherwise>desc</otherwise></choose>
            <if test="first != 0 and count != 0">limit #{first}, #{count}</if>
    </select>

    <select id="selectAllCounterparts" parameterType="long" resultMap="counterpartResultMap">
        select c.*, concat_ws(' ', c.`last_name`, c.`first_name`, c.`middle_name`) as `hname`
          from `counterpart` c where c.`session_id` = #{sessionId}
    </select>

    <select id="selectCounterpartCount" parameterType="org.complitex.flexbuh.common.entity.FilterWrapper"
            resultType="int">
        select count(*) from `counterpart` c
        <include refid="selectCounterpartsWhere"/>
    </select>

    <insert id="insertCounterpart">
        insert into `counterpart` (`session_id`, `person_profile_id`, `hk`, `last_name`, `first_name`, `middle_name`, `hloc`, `htel`, `hnspdv`)
          values (#{sessionId}, #{personProfileId}, #{hk}, #{lastName}, #{firstName}, #{middleName}, #{hloc}, #{htel}, #{hnspdv})
    </insert>

    <update id="updateCounterpart">
        update `counterpart` set `hk` = #{hk}, `last_name` = #{lastName}, `first_name` = #{firstName}, `middle_name` = #{middleName},
        `hloc` = #{hloc}, `htel` = #{htel}, `hnspdv` = #{hnspdv}
          where `id` = #{id}
    </update>

    <delete id="deleteCounterpart" parameterType="long">
        delete from `counterpart` where `id` = #{id}
    </delete>
</mapper>